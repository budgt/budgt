apiVersion: apps/v1
kind: Deployment
metadata:
  name: budgt-account-service
  namespace: budgt-local
  labels:
    app: budgt-account-service
    tier: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: budgt-account-service
  template:
    metadata:
      labels:
        app: budgt-account-service
    spec:
      containers:
        - name: budgt-account-service
          image: "budgt/budgt-account-service:edge"
          ports:
            - containerPort: 1332
              protocol: TCP
          imagePullPolicy: Always
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "local"
      initContainers:
        - name: init-db
          image: busybox
          command:
            [
              "sh",
              "-c",
              "until nc -z budgt-mongodb:27017; do echo waiting for db; sleep 2; done;",
            ]
        - name: init-config-server
          image: busybox
          command:
            [
              "sh",
              "-c",
              "until curl http://budgt-config-server:1339/account-service/dev; do echo waiting for config-server; sleep 2; done;",
            ]
