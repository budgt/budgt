spring:
  application:
    name: category-service
  cloud:
    config:
      uri: http://localhost:1339
      failFast: true
      retry:
          initialInterval: 3000
          multiplier: 1.3
          maxInterval: 5000
          maxAttempts: 20
      profile: local
